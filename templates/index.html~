<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="{{url_for('static',filename = 'design.css')}}">
    <title>ホームページ</title>
  </head>
  <body>
    <h1>ホームページ</h1>
    {% with messages = get_flashed_messages() %}
        {% if messages %}
            <ul>
                {% for message in messages %}
                    <li>{{ message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    {% endwith %}
    <p>
      <div class = "button"><a href="{{url_for('artists')}}">所属アーティスト紹介</a></div><br>
      <div class = "button"><a href="{{url_for('login')}}">ログイン</a></div><br>
      <div class = "button"><a href="{{url_for('customer_add')}}">会員登録</a></div>
    </p>
  </body>
</html>
